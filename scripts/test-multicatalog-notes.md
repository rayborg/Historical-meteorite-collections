# Multi-catalog test harness

Run from the repository root:

```sh
node scripts/test-multicatalog.cjs
```

The real release includes 205 Foote 1912, 697 Ward 1904, and 738 Farrington 1916 facts-only collection entries. Foote has 132 reviewed exact-name mappings and 73 pending observations; Ward has 49 reviewed exact-name mappings and 648 pending observations; Farrington has 469 reviewed exact-name mappings and 269 pending observations. All three use blocked/undetermined zero-page folio policies. The release also includes 560 Merrill 1916, 949 Prior 1923, and 500 Reeds 1937 facts-only collection entries. Merrill has 560 pending observations, Prior has 758 reviewed exact-name mappings and 191 pending observations, and Reeds has 390 reviewed exact-name mappings and 110 pending observations. Runtime dropdown coverage includes the concise labels `Anderson (1913)`, `Kantor (1920)`, and `Astapovich (1938)` alongside the existing catalog labels.

The release also includes 232 Kanagawa 1996 facts-only collection entries from pages 4-22 and 24: 80 meteorite and 152 tektite/natural-glass observations, with controlled descriptions only, 68 reviewed exact-name MetBull mappings, 164 pending observations, and a blocked/undetermined zero-page folio policy. It retains 361 Palache 1926 facts-only collection entries across nine metadata pages, 151-159, with introduction-only page 151 excluded from the eight record-cited pages, 285 reviewed exact-name MetBull mappings, 76 pending observations, and the same blocked folio boundary. It includes 70 Barnes 1940 facts-only collection entries from pages 583-612, with 48 reviewed exact-name MetBull mappings, 22 pending observations without canonical identity, and 2,169 ASU September 2024 facts-only records with 2,166 unique designations, only `91`, `157`, and `607` duplicated, 2,088 reviewed exact-name MetBull mappings, and 81 pending observations without canonical identity.

The current 52-catalog, 19,553-record schema-12 facts-only candidate contains 14,861 reviewed mappings (14,521 resolved and 340 unresolved), 4,692 pending records, and 111 optional specimen-only individual find locations. The schema-5 display manifest declares 3,407 projected parents, 8,410 atomic cards, and 2,877 non-displayed context partitions. Bonn contributes 348 cards across 345 parents; Story and Prior add 983 context-only representation observations with no specimen, mass-filter, or lineage claims.

The harness uses only Node built-ins and the CommonJS exports from `app.js`. Its compact schema-12 fixture covers the closed record models and specimen evidence additions. Mutations reject model widening, malformed contextual facts, invalid locations, and unresolved identity claims. The real release includes one designation-bearing specimen with a source-omitted mass: Appley Bridge `199.1` remains `weight.grams: null`.

The standalone runtime harness has 124 passing tests, and the complete MJS Node suite has 180 passing tests. The suites cover schema-12 and schema-5 mutations, all-record presentation, the closed weight census, source-complete lineage provenance, nullable holding descriptions with neutral `Holding` labels, exact display totals, Brown/Minnesota source-listed behavior, Foote dealer semantics, all 22 Wave 1 lineage routes, search, statistics, privacy, responsive layout, accessibility, issue intake, Hamburg behavior, audited-correction regressions, and the full folio fail-closed matrix.

The search regression sweep runs with `node --test scripts/search-runtime.test.mjs`. It covers 23,393 structured mass occurrences (19,996 distinct record/value pairs), 23,664 searchable mass occurrences (20,036 distinct pairs) including 270 Victoria Table B values, all 14,521 resolved MetBull code owners, all 84 regional card identifiers, 511 H-style classification records, 483 collateral prose records, the complete deterministic union oracle for 6,194 numeric queries, and exact segment result sets for all 1,415 distinct real Huss designation queries. The search audit retains zero omitted owners or oracle drift.

The schema-3 lineage runtime validates 2,439 binary relationships separately from 21 exact n-ary `sourceAttestedGroups`: 194 same-inventory relationships and 2,245 possible matches. The incompatible `139b` singleton is omitted and counted once; every emitted same-inventory pair is identity-consistent. Source-complete routing exposes 963 cards and 1,451 closed claims: 194 known, 1,178 suspected, and 79 tentative.

Victoria contributes 273 Table A specimen cards whose source name is the literal full specimen ID and whose current name is the reviewed canonical name. Exact five-digit searches match only terminal specimen-ID suffixes: 237 suffix groups include 24 duplicate groups covering 60 IDs, and `76009` returns only `ALHA76009`; full, spaced, and hyphenated IDs remain searchable. Table B mass, class, weathering, and fracturing, official canonical names and codes, area-reference coordinates, and visible conflict values are searchable, while mass filters, sorting, and statistics remain Table A-only. Cards preserve the standard Class and Specimen weight rows and add explicit Table A primary and Table B reported values with printed pages for 40 mass, 2 class, and 8 weathering conflicts; all 270 available Table B citations use printed pages rather than PDF positions.

Explicit ordering regressions preserve Nininger catalog items in numeric order, then literal Huss forms, then catalog-number records in source-page order, with collection entries last in increasing `entryOrder`. The runtime rejects collection entries placed before any existing model group. Catalog-number ties use opaque text, nullable name, and ID after the first cited page; no independent monotonic-number rule is applied. Existing parenthesized Huss search and all Nininger behavior remain covered unchanged. Numeric-leading designation search checks both a specimen's top-level designation and catalog-item holdings, including the `26a` public-link regression. The `record id <public-record-id>` mode performs an exact, case-sensitive record lookup without changing ordinary search behavior.

Runtime schema tests intentionally reject pre-12 metadata and malformed schema-12 models. Presenter coverage sweeps all 24,556 display descriptors and all 14,149 specimen cards, locks the closed harmonized DTO and fact order, verifies exact citations, numeric/repeated/qualitative mass evidence, suppresses equivalent or unresolved current-name rows, and verifies lineage routing. It reconciles the default source-listed results to 13,977 specimen descriptors with no observations. Static checks cover contextual representation wording, compact two-column facts with narrow-screen stacking, unchanged card typography, overflow safety, and cache version `20260911-compact-cards-1`.

Folio authorization tests pass the normalized catalog registry to manifest validation, `getAuthorizedFolio`, and `getAuthorizedFolioPages`. They cover matching public-domain policy, blocked policy, malformed manifests, missing or extra catalogs, out-of-range pages, exact `assets/folios/<catalogId>/<pageId>.webp` paths, and independently valid metadata/manifest policies that disagree and therefore deny display.

The validator reports exactly 2 baseline catalog allows, 302 baseline catalog/leakage rejections, 1 model-aware catalog allow, 1 model-ordering/catalog-scope allow, 125 holding-privacy boundary allows, 47 model/holding rejections, 22 catalog-number rejections, 15 collection-entry/schema-12 rejections, 3 MetBull allows, 7 MetBull rejections, 5 folio allows, 51 folio rejections, 2 folio-file allows, and 17 folio-file rejections.

<!-- release-summary:test-notes-folio-lock:start -->
Real-release checks additionally lock reviewed rights evidence, ordered page IDs, and all 49 asset hashes.
<!-- release-summary:test-notes-folio-lock:end -->

The runtime harness checks synthetic behavior; the Node suites and integrated validators additionally inspect the deployed catalog, projection, lineage, and folio files.

<!-- release-summary:test-notes-real-release:start -->
The default `node scripts/validate-public-catalog.mjs` command validates the real schema-12 `data/catalog.json` and `data/folios.json`: 19,553 records across 52 catalogs with 49 displayable folio pages. Runtime projection checks reconcile 24,556 display descriptors and 172 unknown-mass specimen exclusions.
<!-- release-summary:test-notes-real-release:end -->

Real Hovey metadata is `display/public-domain`; Hovey catalog numbers remain searchable but are excluded from `recordsWithDesignation`, matching the descriptor. `--synthetic-only` checks strict schema-12 fixtures without reading deployed files.
