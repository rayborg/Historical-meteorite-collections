# Multi-catalog test harness

Run from the repository root:

```sh
node scripts/test-multicatalog.cjs
```

The real release includes 205 Foote 1912, 697 Ward 1904, and 738 Farrington 1916 facts-only collection entries. Foote has 132 reviewed exact-name mappings and 73 pending observations; Ward has 49 reviewed exact-name mappings and 648 pending observations; Farrington has 469 reviewed exact-name mappings and 269 pending observations. All three use blocked/undetermined zero-page folio policies. The release also includes 560 Merrill 1916, 949 Prior 1923, and 500 Reeds 1937 facts-only collection entries. Merrill has 560 pending observations, Prior has 758 reviewed exact-name mappings and 191 pending observations, and Reeds has 390 reviewed exact-name mappings and 110 pending observations. Runtime dropdown coverage includes the concise labels `Anderson (1913)`, `Kantor (1920)`, and `Astapovich (1938)` alongside the existing catalog labels.

The release also includes 232 Kanagawa 1996 facts-only collection entries from pages 4-22 and 24: 80 meteorite and 152 tektite/natural-glass observations, with controlled descriptions only, 68 reviewed exact-name MetBull mappings, 164 pending observations, and a blocked/undetermined zero-page folio policy. It retains 361 Palache 1926 facts-only collection entries across nine metadata pages, 151-159, with introduction-only page 151 excluded from the eight record-cited pages, 285 reviewed exact-name MetBull mappings, 76 pending observations, and the same blocked folio boundary. It includes 70 Barnes 1940 facts-only collection entries from pages 583-612, with 48 reviewed exact-name MetBull mappings, 22 pending observations without canonical identity, and 2,169 ASU September 2024 facts-only records with 2,166 unique designations, only `91`, `157`, and `607` duplicated, 2,088 reviewed exact-name MetBull mappings, and 81 pending observations without canonical identity.

The current 55-catalog, 19,991-record schema-14 facts-only candidate contains 15,299 reviewed mappings (14,606 resolved and 693 unresolved), 4,692 pending records, and 111 optional specimen-only individual find locations. The schema-6 display manifest declares 3,407 projected parents, 8,410 atomic cards, 2,877 non-displayed context partitions, and 5,058 exact source-reviewed catalog numbers across six catalogs. Bonn contributes 348 cards across 345 parents; Story and Prior add 983 context-only representation observations; Farrington 1915 and Silberrad 1932 add 353 regional-event observations. None make specimen, mass-filter, or lineage claims.

The harness uses only Node built-ins and the CommonJS exports from `app.js`. Its compact schema-14 fixture covers the closed record models and specimen evidence additions. Mutations reject model widening, malformed contextual facts, invalid locations, and unresolved identity claims. The real release includes one designation-bearing specimen with a source-omitted mass: Appley Bridge `199.1` remains `weight.grams: null`.

The standalone CommonJS runtime harness has 124 passing tests. The complete MJS Node suite has 222 passing tests covering schema-14, schema-6, and lineage schema-4 mutations, all-record presentation, exact source-number path/span evidence, the closed weight census, source-complete lineage provenance, grouped cross-catalog comparisons, nullable holding descriptions with neutral `Holding` labels, exact display totals, Brown/Minnesota source-listed behavior, Foote dealer semantics, all 22 reviewed comparison routes, regional-event isolation, search, statistics, privacy, responsive layout, accessibility, issue intake, Hamburg behavior, audited-correction regressions, current-context isolation, and the full folio fail-closed matrix.

The source-only search regression sweep runs with `node --test scripts/search-runtime.test.mjs`. It covers all structured and searchable source masses, including 270 Victoria Table B values and 85 Antarctic Appendix masses, all regional source-number or internal-order queries, H-style classification owners, the deterministic numeric-query union oracle, and exact real Huss designation result sets. Reviewed current identity names and codes do not leak into that source index. The separate current-context test checks 59,286 populated current-context values and 29,984 historical catalog-note rows while preserving the source-only oracle.

The Antarctic release tests lock the `appendix-specimen` contract to `antarctic-1980`, exactly 85 source-order rows from Appendix 1 on printed pages 47-48, the 54/31 page split, 89,891.1 g mass total with no null masses, and retained null chemistry/weathering values. They reject Appendix 2 scope, malformed fields, inferred nulls, folio authorization, and non-`ALHA77` identifiers. They also require 85 independent later-Victoria continuity links, no record merge or custody/ownership/transfer claim, no Antarctic comparison candidates, and no lineage change from current-context sidecar presence or content.

The schema-4 runtime validates 279 same-inventory relationships, including exactly 85 Antarctic-to-Victoria links, and 21 exact n-ary `sourceAttestedGroups` as true lineage, fully separate from the unchanged 1,541 cross-catalog comparison groups containing 2,245 candidate pairings. The incompatible `139b` singleton is omitted and counted once; every emitted same-inventory pair is identity-consistent. Source-complete lineage routing exposes exactly 303 cards and 358 claims: 279 known and 79 tentative. Comparison indexing is symmetric across 2,069 source records and 3,668 distinct group/record/mass-path routes; exact emitted-card routing exposes 1,717 cards and 1,889 top-level group occurrences without leaking to sibling mass cards.

Victoria contributes 273 Table A specimen cards with literal full specimen IDs and searchable source facts. Exact five-digit searches match only terminal specimen-ID suffixes: 237 suffix groups include 24 duplicate groups covering 60 IDs, and `76009` returns only `ALHA76009`; full, spaced, and hyphenated IDs remain searchable. Table B mass, classification, weathering, and fracturing, area-reference coordinates, and visible conflict values remain in the source search index; reviewed canonical names and codes do not. With valid current context, every card uses the linked Official heading and primary current MetBull classification, place, fall/find, and year. The historical Table A classification remains under collapsed catalog notes when it differs, while Table A specimen weight remains a primary specimen fact. Explicit Table A/Table B values and printed pages remain visible for 40 mass, 2 classification, and 8 weathering conflicts; all 270 available Table B citations use printed pages rather than PDF positions. Mass filters, sorting, and statistics remain Table A-only.

Explicit ordering regressions preserve Nininger catalog items in numeric order, then literal Huss forms, then catalog-number records in source-page order, with collection entries last in increasing `entryOrder`. The runtime rejects collection entries placed before any existing model group. Catalog-number ties use opaque text, nullable name, and ID after the first cited page; no independent monotonic-number rule is applied. Existing parenthesized Huss search and all Nininger behavior remain covered unchanged. Numeric-leading designation search checks both a specimen's top-level designation and catalog-item holdings, including the `26a` public-link regression. The `record id <public-record-id>` mode performs an exact, case-sensitive record lookup without changing ordinary search behavior.

Runtime schema tests intentionally reject pre-14 metadata and malformed schema-14 models. Presenter coverage sweeps all 24,994 display descriptors and all 14,234 specimen cards, locks the closed harmonized DTO and known-fact order, verifies exact citations and numeric/repeated/qualitative mass evidence, and requires a concise catalog shortname on every card. Exactly 13,631 typed source identifiers retain their wording, 5,058 projected cards append exact projection-bound numbers, and 6,305 cards display the shortname alone; 3,153 internal entry-order values never become identifiers. It suppresses all complete field-aware unavailability statements while retaining all 64 compound evidence values, verifies kind-correct populated-name branches and null-name fallbacks, and verifies exact lineage and comparison routing. It reconciles the default source-listed results to 14,062 specimen descriptors with no observations. Lineage disclosures name the earlier catalog without rendering stable IDs or unavailable endpoint rows; comparison disclosures remain separately labeled and carry the physical-identity, lineage, custody, ownership, transfer, and merge warning. Static checks cover source-catalog classification labeling, cross-catalog duplicate-number disambiguation, concise name labels, contextual representation wording, compact two-column facts with narrow-screen stacking, unchanged card typography, overflow safety, and cache version `20260913-catalog10-historical-1`.

The exhaustive null-source-name census contains 70 cards: 62 have a genuine typed or evidence-bound source identifier and use `Source catalog identifier`; 8 have only the universal concise shortname and use `Source catalog record`. Both branches reuse that heading value and remove the duplicate top designation line.

The current-context coverage audit partitions all 14,234 specimen cards into 11,859 mapped and 2,375 unmapped cards, using 2,548 Official codes across 7,736 mapped parents. Mapped cards lead with current Official heading, classification, place, year, and fall/find context. Historical source fields remain searchable and appear under collapsed `Show catalog notes` when they differ; equivalent context is not duplicated. Observations and unmapped cards remain source-only. Official mass, coordinates, comments, and raw CSV data are unavailable to presentation.

Run `node --test scripts/metbull-lineage-isolation.test.mjs` for release isolation. It executes the lineage builder with the accepted sidecar, no sidecar, and changed current context, locking exact SHA-256 `0f99792c052527ed9a00690b989ea9e6980f12701d07b75c70e54f55874dcc04` plus candidate IDs, evidence strength, grouping, and counts. Classification changes cannot affect likelihood because comparison identity remains the already reviewed MetBull event code plus source-reported mass. `Y` displays as `Fall`, empty as `Find`, and exceptional `Yc`, `Yp`, and `Np` as literal `Code <value>`; no mapped card currently uses unsupported `Nd`.

Folio authorization tests pass the normalized catalog registry to manifest validation, `getAuthorizedFolio`, and `getAuthorizedFolioPages`. They cover matching public-domain policy, blocked policy, malformed manifests, missing or extra catalogs, out-of-range pages, exact `assets/folios/<catalogId>/<pageId>.webp` paths, and independently valid metadata/manifest policies that disagree and therefore deny display.

The validator reports exactly 2 baseline catalog allows, 302 baseline catalog/leakage rejections, 1 model-aware catalog allow, 1 model-ordering/catalog-scope allow, 125 holding-privacy boundary allows, 47 model/holding rejections, 22 catalog-number rejections, 15 collection-entry/schema-14 rejections, 3 MetBull allows, 7 MetBull rejections, 5 folio allows, 51 folio rejections, 2 folio-file allows, and 17 folio-file rejections.

<!-- release-summary:test-notes-folio-lock:start -->
Real-release checks additionally lock reviewed rights evidence, ordered page IDs, and all 49 asset hashes.
<!-- release-summary:test-notes-folio-lock:end -->

The runtime harness checks synthetic behavior; the Node suites and integrated validators additionally inspect the deployed catalog, projection, lineage, and folio files.

<!-- release-summary:test-notes-real-release:start -->
The default `node scripts/validate-public-catalog.mjs` command validates the real schema-14 `data/catalog.json` and `data/folios.json`: 19,991 records across 55 catalogs with 49 displayable folio pages. Runtime projection checks reconcile 24,994 display descriptors and 172 unknown-mass specimen exclusions.
<!-- release-summary:test-notes-real-release:end -->

Real Hovey metadata is `display/public-domain`; Hovey catalog numbers remain searchable but are excluded from `recordsWithDesignation`, matching the descriptor. `--synthetic-only` checks strict schema-14 fixtures without reading deployed files.
