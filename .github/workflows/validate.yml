name: Validate public catalog

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: 24
      - run: node scripts/build-specimen-lineages.mjs --check
      - run: node scripts/validate-specimen-lineages.mjs
      - run: node scripts/validate-public-catalog.mjs
      - run: node scripts/test-multicatalog.cjs
      - run: node --test scripts/*.test.mjs
